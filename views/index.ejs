<!DOCTYPE html>
<html lang="en">

<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>FindOs</title>
    <link rel="icon" type="image/png" href="../assets/fav.png">
    <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.1/dist/css/bootstrap.min.css" rel="stylesheet"
        integrity="sha384-4bw+/aepP/YC94hEpVNVgiZdgIC5+VKNBQNGCHeKRQN+PtmoHDEXuppvnDJzQIu9" crossorigin="anonymous">
</head>

<body>
    <!-- navgiation -->
    <nav class="navbar navbar-expand-lg shadow bg-light">
        <div class="container-fluid">
            <a class="navbar-brand fw-bold" href="/">Find<span class="text-danger">O</span>s</a>
            <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav"
                aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
                <span class="navbar-toggler-icon"></span>
            </button>
            <div class="collapse navbar-collapse" id="navbarNav">
                <!-- <ul class="navbar-nav">
                <li class="nav-item">
                    <a class="nav-link active fw-bold" aria-current="page" href="/">Home</a>
                </li>
                <li class="nav-item">
                    <a class="nav-link" aria-current="page" href="/logout">Logout <i
                            class="fa-solid fa-power-off power"></i></a>
                </li>
            </ul> -->
            </div>
        </div>
    </nav>

    <div class="container mt-5 text-center">
        <h1 class="text-center">Find Your Operating System</h1>
        <div class="row justify-content-center my-5">
            <div class="col-md-6">
                <a href="/os" id="findOsBtn" class="btn btn-primary btn-block">Find Your OS</a>
            </div>
        </div>
        <div class="row mt-3 d-flex justify-content-center align-items-center">
            <% if(osResult){ %>
                <div class="col-md-6">
                    <div id="osResult" class="alert alert-info text-start">
                        <p><span class="fw-bold">Operating System</span> : <%=osResult.osName %></p>
                        <p><span class="fw-bold">Platform </span> : <%=osResult.osPlatform %></p>
                        <p><span class="fw-bold">Release </span> : <%=osResult.osRelease %></p>
                        <p><span class="fw-bold">Architecture</span> : <%=osResult.osArchitectue %></p>
                        <p><span class="fw-bold">Total RAM Memory</span> : <%=osResult.osTotalMem %> MB</p>
                        <p><span class="fw-bold">Free RAM Memory</span> : <%=osResult.osFreeMem %> MB</p>
                        <p><span class="fw-bold">User Info</span> : <%=osResult.osUserInfo.username %></p>
                    </div>
                </div>
                <% } %>
        </div>
    </div>

</body>

</html>